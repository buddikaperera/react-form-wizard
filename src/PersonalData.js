import React from "react";

function PersonalData(props) {
  return (
    <div>
      <h2>1 PersonalData</h2>
      <button onClick={() => setStep(props.step + 1)}>ddddd </button>
    </div>
  );
}

export default PersonalData;
